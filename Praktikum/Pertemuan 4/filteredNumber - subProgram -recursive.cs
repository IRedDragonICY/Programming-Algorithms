using System;
using System.IO;
using System.Text;

namespace filteredNumber - subProgram -recursive
{
   public class main_class
   {
      static System.Random random_generator = new System.Random();
      public static void recursiveFilteringNumber (
            ref  ??_Variable indexFilteredNumber;
            ref  ??_Variable indexIteration;
            ref  ??_Variable totalFilteredNumber;
            ref  ?? filteredNumber)
      {
         ?? totalfilterednumber;
         ?? miniteration;
         ?? indexfilterednumber;
         ?? maxiteration;
         ?? indexiteration;
         ??[] filterednumber = new ??[??+1];
      
         minIteration =1;
         maxIteration =100;
         if (indexIteration % 5==0 && indexIteration % 7==0)
         {
            filteredNumber[indexFilteredNumber] = indexIteration;
            indexFilteredNumber =indexFilteredNumber+1;
            totalFilteredNumber =totalFilteredNumber+1;
         }
         else
         {
         }
